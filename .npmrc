# Enable workspace package injection for pnpm deploy
inject-workspace-packages=true

# Optimize for Docker builds
prefer-frozen-lockfile=true

# Allow build scripts (needed for better-sqlite3 native compilation)
enable-pre-post-scripts=true
